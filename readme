load_data.pl вгружает в БД (mysql,настройки берутся из gconf.yaml) данные из файла out.
Исходя из заданных условий создания таблицы некоторые логи с флагом'<=' пропускаются скриптом посколько в них нету id ,
который считается primary key.Также в задании не указано что именно означает поле status в таблице message, 
я додумал,что это статус доставленного сообщения (1 - если доставлено (есть флаг =>),0 во всех других)

gtest - веб приложение на основе mojolicious lite(можно установить через cpan) - включается с помощью команды morbo gtest.
приложение открывается по адресу /index (например localhost:3000/index в моем случае)




